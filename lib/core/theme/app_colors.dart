import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary agricultural palette
  static const Color primary = Color(0xFF03AC0E); // Vibrant e-commerce green
  static const Color primaryLight = Color(0xFF5DF356);
  static const Color primaryDark = Color(0xFF007A00);
  static const Color accent = Color(0xFFFF6D00); // Vibrant orange for highlights

  // Background and Surface
  static const Color background = Color(0xFFF9FAFB); // Clean off-white/light grey
  static const Color surface = Color(0xFFFFFFFF);
  
  // Text Colors
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Status Colors (Cashflow & Notifications)
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFA000);
  static const Color info = Color(0xFF1E88E5);

  // Borders & Dividers
  static const Color border = Color(0xFFE0E0E0);
}
